BACKGROUND_HISTORY = 10 # Frames used for background context
BUFFER_DUR = 3 # Seconds to keep in rolling buffer
CONF = 0.2 # Minimum detection confidence
EXCLUDED_OBJECTS = "person" # Object names to exclude from recording
FPS = 4 # Capture frames per second
FRAME_HEIGHT = 1296 # Camera frame height in pixels
FRAME_WIDTH = 2304 # Camera frame width in pixels
DETECTION_IMGSZ = 640 # Detection model input image size
EMBEDDING_IMGSZ = 320 # Embedding model input image size
LOG_LEVEL = "DEBUG" # Logging verbosity
MAX_DETS = 5 # Max detections per frame
NMS_IOU_THRESHOLD = 0.3 # IoU threshold for NMS
MJPEG_QV = 10 # MJPEG quality value
MODEL_DETECTION_PATH = "yolo26n_finetune_ncnn_model" # Detection model location
MODEL_EMBEDDING_PATH = "shufflenetv2_x0_5_embeddings_onnx_model" # Embedding model location
MONITORING_PERIOD = 1 # Seconds between monitoring checks
MONITORING_DISK_WRITE_WARN_MB_S = 20 # Warn when disk throughput exceeds this MB/s in one monitoring interval
MONITORING_QUEUE_GROWTH_WARN = 2 # Warn when frame queue grows by at least this many items in one interval
MONITORING_RSS_GROWTH_WARN_MB = 40 # Warn when RSS memory rises by this many MB in one interval
MONITORING_STALE_DUMP_INTERVALS = 3 # Number of consecutive stale processing heartbeat intervals before dumping thread stacks
OUTPUT_DIR = "object_clips" # Output folder for clips/logs
SAVE_RAW_VIDEO = "both" # Raw-video mode: no (annotated only) | only (raw only) | both (annotated + raw)
TRACK_HISTORY_DUR = 5 # Seconds to retain track history
TRACK_NEW_CONF_THRESHOLD = 0.4 # Confidence needed for new tracks
TRACK_NEW_DUR = 2 # Allowable duration in new state
TRACK_MATCH_THRESHOLD = 0.55 # Minimum match score required